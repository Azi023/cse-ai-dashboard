# Fix, Test & Validate Report — March 22, 2026

Generated by autonomous session executing `docs/fix-test-validate-prompt.md`.

---

## Data Freshness Status

| Source | Last Updated | Status | Notes |
|--------|-------------|--------|-------|
| CSE Market (ASPI) | 2026-03-20 (last trading day) | ✅ | ASPI 20,639.73. Weekend — no trading. Refreshed during market hours on Friday. |
| Stock Prices | 2026-03-22T10:00 UTC | ✅ | Stocks updated in DB, live prices from Redis trade summary during market hours |
| Global Markets (Oil, Gold, FX, S&P) | 2026-03-22 | ✅ | **Fixed**: Was stale at Mar 10. Manual refresh restored data. Auto-refresh-on-stale logic added. |
| News (RSS) | 2026-03-20 | ✅ | 50 articles. Latest: "Sri Lankan shares close higher, but post weekly losses" (Mar 20). Weekend — no new articles expected. |
| AI Brief | 2026-03-22T08:10 UTC | ✅ | Generated today. Data-constrained (weekend/market closed) — correctly states "MARKET PULSE — 22 Mar 2026, NEUTRAL". |
| Macro Data (CBSL) | 2026-03-20 | ✅ | ASPI: 20639.73, AWPLR: 10.45%, PE ratio: 10.1x. Source: manual + cbsl_fallback. |
| ForexFactory Calendar | 2026-03-22 | ✅ | 10 high-impact events this week. Redis-cached 4h. |

---

## Feature Test Results

| Feature | Status | Notes |
|---------|--------|-------|
| Goal templates (Emergency Fund) | ✅ | Clicking template pre-fills form with LKR 500,000 label and opens goal creation form |
| Goal templates (Grow Monthly SIP) | ✅ | Pre-fills LKR 300,000 with label |
| Goal templates (15% Return Target) | ✅ | Pre-fills LKR 115,000 with label |
| Custom goal creation (+ Set Goal) | ✅ | POST /api/journey/goals with camelCase `targetAmount` and `label` works. Goal persists in DB. |
| Record monthly deposit | ✅ | POST /api/journey/deposit works. Endpoint is `/deposit` not `/deposits`. |
| Simple mode AI brief | ✅ | **Fixed**: Was showing raw "DATA UNAVAILABLE" markdown. Now shows friendly weekend/data-constrained message. |
| Pro mode AI banner (data-constrained) | ✅ | **Added**: Blue info banner appears at top of brief when data is unavailable. |
| Pro mode AI table rendering | ✅ | MarkdownRenderer component already in use in DailyBriefCard — tables render as HTML. |
| Portfolio health scores | ✅ | All 5 scores validated — see details below. |

---

## Portfolio Health Score Validation

Scores from `GET /api/journey/health`:

| Metric | Score | Label | Formula | Verdict |
|--------|-------|-------|---------|---------|
| Diversification | 20/100 | Highly concentrated | HHI index + stock/sector count. 1 stock = near-max concentration. | ✅ Correct |
| Shariah Compliance | 100/100 | Fully Shariah compliant | % of portfolio value in compliant stocks. AEL = compliant. | ✅ Correct |
| Risk Level | 75/100 | Moderate risk | Based on volatility, position sizing, sector exposure. | ✅ Reasonable for single-stock portfolio |
| Cost Efficiency | 35/100 | Slight loss | Portfolio P&L-based. Current: -5.37% (LKR -755.69). Scores below 50 = loss zone. | ✅ Correct |
| Consistency | 30/100 | Building habit — 1 month streak | Consecutive deposit streak. User has 1 month (Mar 2026). | ✅ Correct |
| Overall | 55/100 | D | Weighted: diversification×0.2 + shariah×0.25 + risk×0.2 + efficiency×0.15 + consistency×0.2 | ✅ Correct |

---

## Fixes Applied

### Task 1 — Global Market Data Staleness
**File:** `src/backend/src/modules/global-data/global-data.service.ts`
- Added staleness check in `getGlobalIndicators()` — if Redis-cached data or DB data is older than 2 days, triggers a background async refresh via `fetchAllGlobalData()`
- Root cause: Backend restart cleared the running process; cron only runs weekdays so stale weekend data persisted until manually triggered
- Manual refresh via `POST /api/global/refresh` confirmed working. Data now shows 2026-03-22

### Task 2 — Simple Mode AI Brief
**File:** `src/frontend/src/components/market/simple-dashboard.tsx`
- Added `simplifyAIBrief()` function that:
  - Returns friendly weekend/loading message when brief contains `DATA UNAVAILABLE` or `data-constrained`
  - Translates technical sentiment (BULLISH/NEUTRAL/BEARISH/CAUTIOUS) to plain English
  - Extracts Base case / Recommendation section from raw brief
  - Filters out placeholder "See analysis above" opportunity text

### Task 3 — Goal Features Validated (No Code Changes Needed)
- Goal creation backend `POST /api/journey/goals` requires camelCase body: `{ targetAmount, label, targetDate? }`
- Templates correctly wire to form → submit to API → persist in DB
- Deposit endpoint is `/api/journey/deposit` (singular, not `/deposits`)
- All goal features are fully functional end-to-end

### Task 4 — Pro Dashboard Data-Constrained Banner
**File:** `src/frontend/src/components/market/daily-brief.tsx`
- Added blue info banner at top of brief when summary contains `DATA UNAVAILABLE` or `data-constrained`
- Message: "Market is closed. This analysis uses the most recent available data. Real-time data resumes the next trading day at 9:30 AM SLT."

### Task 5 — Data Pipeline Validation
- All data sources healthy. No fixes required.
- ForexFactory economic calendar endpoint: `GET /api/global/economic-calendar` ✅ (10 events)
- `/api/market/snapshot` returns 404 — snapshot accumulation not yet built (expected, see CLAUDE.md Task 2.1)
- Macro endpoint is `/api/macro/indicators` not `/api/macro` — documented

### Task 6 — Color Audit
- No hardcoded problematic colors found
- All `text-white` occurrences are intentionally on colored backgrounds (buttons, badges)
- Single `bg-gray-500` in signals/page.tsx is a neutral progress bar — intentional
- Codebase is theme-aware throughout

### Task 7 — Portfolio Health Scores
- All scores validated against actual user data (1 stock AEL, -5.37% P&L, 1-month streak)
- Formula documented above — all correct

---

## Remaining Issues / Known Gaps

| Issue | Severity | Notes |
|-------|----------|-------|
| `/api/market/snapshot` 404 | LOW | Expected — market snapshot accumulation not built yet (Phase 2, Task 2.1) |
| `/api/journey/deposits` 404 | LOW | Correct endpoint is `/api/journey/deposit` (POST only, no GET list). Frontend uses correct endpoint. |
| ATrad stock holdings shows 0 | MEDIUM | Known issue from CLAUDE.md — T+2 settlement. Requires manual sync verification. |
| Stock scoring needs 20 market days | INFO | Accumulating since Mar 19. Meaningful scores ~Apr 16, 2026. |
| Brent Crude showing $106 (vs ~$70 real) | INFO | Yahoo Finance `BZ=F` ticker may be returning a futures contract price. Data is from real API, not mock. |
